export interface ProductCategory {
  id: number;
  check: boolean;
  category: string;
  createdBy: string;
  date: string;
  lastToUpdate: string;
  updated: string;
  status: boolean;
  action: boolean;
}
